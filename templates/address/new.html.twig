{% extends 'base.html.twig' %}

{% block title %}New Address{% endblock %}

{% block body %}
{{ include("partials/title_section.html.twig", {'name':'Add Address'}) }}
    <div class="login_register_wrap section">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-xl-6 col-md-10">
                    <div class="login_wrap">
                        <div class="padding_eight_all bg-white">
                            <div class="heading_s1">
                                <h3>Add Address</h3>
                            </div>
                            <div class="content">
                                  {{ include('address/_form.html.twig') }}
                                  <a class="btn btn-border-fill btn-block mt-2" href="{{ path('address_index') }}">back to account</a>
                            </div>                           
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
  {# <h1>Create new Address</h1> #}

    {# {{ include('address/_form.html.twig') }} #}

    {# <a href="{{ path('address_index') }}">back to list</a> #}
{% endblock %}
